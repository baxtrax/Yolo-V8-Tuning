# This contains the training configuration
data: coco128.yaml  # REQUIRED, dataset.yaml path
args: # List of these args can be found here: https://docs.ultralytics.com/usage/cfg/#train
  batch: 16  # OPTIONAL batch size
  epochs: 1  # OPTIONAL Number of epochs